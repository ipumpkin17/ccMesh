# ccMesh 更新清单

## 新增功能

- 端点模型列表获取与可用性探测
- 启动行为设置：静默启动、开机自启、自动运行代理
- 端点模型点亮态（标记常用模型）
- 模型映射按点亮模型过滤候选
- 应用单例与桌面快捷唤起
- 端点卡片 API URL 点击跳转浏览器
- 端点类型筛选增加 Codex

## 修复与优化
- 修复mac图标边距导致图标偏大问题
- 应用代理端口不一致导致的失效问题
- 代理端口统一读取 `port` 配置键，保存后同步刷新状态
- Codex 默认 TOML 端口随设置动态更新
- 弹窗限高并支持内容滚动
- macOS Dock 图标点击恢复并聚焦主窗口
- `/v1/models` 跨端点模型去重，防止配置模型获取重复
- macOS 下载更新完成后自动重启应用载入新版
- 禁用代理层 Axum 默认 2MB body 限制，大上下文 LLM 请求不再被 413 截断
- 端点管理页/顶部导航隐藏滚动条且不影响其他页面
- 顶部导航横向列表支持纵向滚轮映射为左右滚动
- 隐藏密码输入框浏览器原生显隐按钮，避免与自定义眼睛图标重复
- 全局 toast 提示移至右上角并避开标题栏
- 模型映射弹窗描述优化，出站说明改为图标提示
- 统计页历史记录弹窗表格溢出修复（覆盖默认 sm:max-w-lg 限制）
- 修复 codex 多轮历史切到 openai chat 端点时，function_call.arguments 残缺 JSON 原样透传导致上游报 `prefill failed: unexpected end of data`：转换层增加 JSON 完整性校验，合法规范化、残缺降级为 `{}` 并告警
- 修复 token 用量合计双重计算 cache_read：OpenAI 风格 input_tokens 已含 cached_tokens，统一归一化为净输入（`max(input - cache_read, 0)`），覆盖非流式、透传流式及 Chat→Responses/Claude 两条转换流式路径

## 新增功能（2026-06）
- 端点管理页改为中右布局：中间端点列表居中（max-w-4xl），右侧侧栏封装端点统计 + 可用模型
- 端点卡片与可用模型列表显示品牌图标（移植 @lobehub/icons，覆盖 40 组品牌前缀）
- 可用模型 Badge 点击复制模型名，标题加 InfoIcon 提示"按启用端点"
- 端点卡片按 transformer 类型显示品牌图标（claude→Anthropic、openai→OpenAI、codex→Codex）
- 请求监控列表端点列按 transformer 显示品牌图标（落库快照，旧行回退入站协议）
- 请求日志落库端点 transformer 快照（migration v11），端点后续改类型不影响旧日志
- 端点卡片 URL 对齐标题文字并去掉悬停下划线



