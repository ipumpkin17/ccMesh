仪表盘：
1：本地代理端口没有正确读取设置填写的端口值
2：检查设置填写的代理端口没有生效，仅仅是检查

日志体现
15:28:57.426
INFO
proxy::server
代理服务已启动port=3000
16:13:50.758
INFO
proxy::server
代理服务已启动port=3002

具体表现是在仪表盘关闭本地代理后 3002 ->变为3000 ，再次启动后，配置的端点信息和仪表盘端点信息不一致
3：需要检查用到这个端口的配置，在新建配置的时候也正确读取端口信息。


4当前软件没有限制单例模式，请研读项目F:\IT\cockpit-tools ，借鉴其单例模式，点击桌面快捷能正确唤起，window和mac linux 都得实现响应的唤起和单例


5我想知道
src-tauri\icons 下的icon.icns有没有正常工作

6：统计统计的历史记录数值溢出弹窗，如果数据过大的话，会出现这种问题，看看是不是应该调整弹层宽度 ，具体查阅：统计的历史记录数值溢出弹窗.png

7：当前如果小视图的情况下，会出现弹窗显示不全的问题，是否需要修复，请参考：是否应该支持弹窗在页面滚动.png

8：端点管理页面的每个端点卡片显示的 API URL 能否改为直接点击跳转浏览器打开。评估是否可以简单实现，不用配置，如果有问题，就说明情况，否则不要改动

9:模型清单，一键拉取模型，模型会回填到当前输入框，回填的是标签，标签也能移除，但是需要添加一个状态（点亮状态），点亮标签则视为需要的，灰色的则视为保留的不参与展示的，如果一个都没点亮，就默认全部使用，这是兼容已经上线的项目，将点亮状态作为可选值，需要做好回显。