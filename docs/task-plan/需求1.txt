优先级方案:
 在仪表盘的端点队列中，添加一个快速队列编辑的入口，放卡片的右边位置
1:端点队列，指在端点管理列表全部端点

2:启用队列，启用的端点

3:优先队列，启用的端点，进行分配

圆点⚪改为  fast   ⚡图标，颜色还是用

之前的工作状态颜色

需求描述：

表添加两个字段，是否快速类型，快速排序

1: 仪表盘界面，如果存在快速队列的端点，则展示一分为二左右卡片，左边为快速队列，右边为启用队列。

2: 启用队列的排序，不影响快速队列的排序

3:只有启用的端点，才能通过编辑页面调整是否转换为快速类型

4:编辑弹窗

左列卡片为快速队列，右边为启用队列，左右编辑弹窗内卡片容器能互相拖动队列里的元素。

左边能排序，右边不能排序，是为了保护端点整体排序

5:左边编辑端点能通过点击 x 快速移除元素，回到启用队列

所以端点的轮询策略改为快速队列存在就只轮询快速队列，不存在就轮询启用队列只，