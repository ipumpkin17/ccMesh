F:\IT\cc-switch 配置需要迁移到当前项目的端点管理卡片资料

配置同步的入口在同步页面，放在顶部，webdav放下面，使用卡片设置区分，上下两个卡片
点击同步配置会读取cc-switch响应的数据源，，弹出识别的端点信息，允许用户针对能识别的源信息来进行勾选应用同步，没有勾选的就不进行同步，
有全选按钮，方便快捷选择，勾选的和总数会显示出来，这个是界面的展示，后端的逻辑通过梳理自行实现。