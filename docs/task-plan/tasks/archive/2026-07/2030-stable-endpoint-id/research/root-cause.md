# 名称被误用为端点身份的根因

## 现象

端点名称从 A 改成 B 后，模型列表中的端点标识、运行期当前端点、熔断状态以及统计分组都随名称变化。配置导入和 WebDAV 也按名称判断同一端点。

## 根因

数据库只有本机自增主键和唯一名称。自增主键无法跨配置导入和设备同步保持一致，于是多个模块退而使用名称作为键。名称是用户可编辑展示字段，不具备身份稳定性。

## 修复边界

新增独立 UUID 作为稳定身份，同时保留数值主键避免扩大本机 CRUD 改造。历史统计无法可靠推断改名前后的真实身份，因此不回填；迁移只给新列默认空值，所有后续新数据强制写 UUID。
