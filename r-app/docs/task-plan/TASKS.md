# TASKS.md — 统计页面与仪表盘改造

> 关联 PRD：[PRD.md](./PRD.md)
> 进度跟踪：[progress.csv](./progress.csv)
> 原始需求：[new_features_task.txt](./new_features_task.txt)

## 工作包总览

| 阶段 | 工作包 | 子文档 | 前置 | 说明 |
|---|---|---|---|---|
| WP1 | 数据层地基 | [01-data-layer.md](./01-data-layer.md) | 无 | 缓存 Token、request_logs 明细表、历史记录命令、采集双写、实时事件（关键路径）|
| WP2 | 统计页改造 | [02-statistics-page.md](./02-statistics-page.md) | WP1 | 顶部 Tab、端点表缓存列、历史记录弹窗、下半实时监控（ranged）|
| WP3 | 用量统计 MVP | [03-usage-stats.md](./03-usage-stats.md) | 无（与 WP1 解耦，可并行）| 读本机 Claude Code/Codex JSONL，增量同步，按 app/模型/天聚合 |
| WP4 | 仪表盘改造 | [04-dashboard.md](./04-dashboard.md) | WP1 | 左3右2 卡片、海水涨潮、服务状态合并、实时统计、底部监控（live）|

构建顺序：WP1 先行 → WP2 监控 / WP4 监控 / WP4 布局并行 → WP3 全程可并行 → 海水涨潮动效最后打磨。

## 任务清单

### WP1 数据层（后端 + 共享类型）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 1.1 | `daily_stats` 增加 `cache_creation_tokens`/`cache_read_tokens` 两列（v3 迁移）| 后端 | — | 4,5 |
| 1.2 | Token 解析补充缓存字段（非流式 + 流式累加）| 后端 | 1.1 | 5 |
| 1.3 | 聚合器/仓储/模型结构体同步缓存字段（含周期与历史查询）| 后端 | 1.1,1.2 | 3,4 |
| 1.4 | 新建 `request_logs` 明细表 + v3 迁移（含字段与索引）| 后端 | 1.1 | 9-14,17 |
| 1.5 | 采集点双写：透传 model/状态码/时间戳/入站格式/出站URL，写明细行 | 后端 | 1.4 | 9-13 |
| 1.6 | `get_request_logs`（分页 + 时间段过滤）命令 | 后端 | 1.4 | 15,16 |
| 1.7 | `request-logged` 实时事件（payload 单条明细）| 后端 | 1.5 | 35 |
| 1.8 | `request_logs` 90 天保留与清理 | 后端 | 1.4 | 17 |
| 1.9 | 历史记录命令：`get_stats_history`（分页）+ `delete_daily_stat`/`delete_stats_by_date` | 后端 | 1.3 | 6,7,8 |
| 1.10 | 前端 services/types 同步（缓存字段、RequestLog 类型、新命令封装、事件订阅）| 前端 | 1.6,1.7,1.9 | 36 |

### WP2 统计页（前端）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 2.1 | 顶部一级 Tab：端点统计 \| 用量统计；原周期 Tab 降为子级 | 前端 | — | 1,2 |
| 2.2 | 端点统计表新增"缓存创建/缓存读取"两列 | 前端 | 1.10 | 3,4 |
| 2.3 | 通用分页组件（新增基础组件）| 前端 | — | 7,16,33 |
| 2.4 | 历史记录改弹窗：分页 + 按行/按天删除 | 前端 | 1.10,2.3 | 6,7,8 |
| 2.5 | 共享 `RequestMonitor` 组件（live/ranged + Token 悬停明细）| 前端 | 1.10,2.3 | 9-16,36 |
| 2.6 | 端点统计下半接入 `RequestMonitor`（ranged + 时间段选择）| 前端 | 2.5 | 9-16 |

### WP3 用量统计（后端 + 前端，MVP）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 3.1 | 用量表 + 同步进度表迁移 | 后端 | — | 18-24 |
| 3.2 | Claude Code 读取器（`~/.claude/projects` JSONL，按 message.id 去重，增量）| 后端 | 3.1 | 18,24 |
| 3.3 | Codex 读取器（`~/.codex/sessions` JSONL，累计取差，增量）| 后端 | 3.1 | 19,24 |
| 3.4 | 聚合查询命令：`sync_session_usage`/`get_usage_summary`/`get_usage_by_model`/`get_usage_by_day` | 后端 | 3.2,3.3 | 20-25 |
| 3.5 | 用量统计前端：总量卡片 + 按模型表 + 按天表 + app_type 过滤 + 手动刷新 | 前端 | 3.4 | 20-25 |

### WP4 仪表盘（前端）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 4.1 | 第一卡片改左3右2 栅格 | 前端 | — | 26,27 |
| 4.2 | 左侧启用端点列表 + 当前工作端点高亮 | 前端 | 4.1 | 26,34 |
| 4.3 | 右侧本地代理信息 + 开关 + 端口点击跳设置 | 前端 | 4.1 | 27,28 |
| 4.4 | 海水涨潮动效（代理 running 时，motion 实现）| 前端 | 4.3 | 29 |
| 4.5 | 服务状态并入第一卡片 + 删除"端点数量"卡片 | 前端 | 4.1 | 30 |
| 4.6 | 原位置改请求数/失败数/Token 实时（订阅 stats-updated）| 前端 | — | 31 |
| 4.7 | 底部"端点+密钥"卡片替换为 `RequestMonitor`（live + 分页 + 别名）| 前端 | 2.5,1.10 | 32-35 |

## 提交策略

按工作包/模块/目录分批提交，使用 git-commit-bot：
- WP1 后端迁移与解析、仓储、命令/事件、清理可分多次提交（迁移+模型一组、解析一组、明细表+采集一组、命令+事件一组）。
- WP2/WP4 前端按"组件/页面"提交；共享 `RequestMonitor` 单独成提交。
- WP3 后端读取器与命令一组、前端展示一组。

---

# TASKS（第二轮）— 监控明细、用量统计与 Token 单位优化

> 关联 PRD：[PRD-2.md](./PRD-2.md)
> 原始需求：[需求 2.txt](./需求%202.txt)
> 背景：第一轮 WP1–WP4 已完成，本轮为针对性修复与增强。

## 工作包总览

| 阶段 | 工作包 | 子文档 | 前置 | 说明 |
|---|---|---|---|---|
| WP5 | 前端工具与监控修复 | [05-frontend-fixes.md](./05-frontend-fixes.md) | 无 | Token 单位工具函数、稳定日期工具、ranged 监控一直加载修复、仪表盘 Token 单位、历史弹窗放大（R4/R6/R5/R7）|
| WP6 | 用量统计增强 | [06-usage-enhance.md](./06-usage-enhance.md) | WP5（5.1 工具 / 5.2 日期）| 用量同步异步化（不卡死）、时间筛选器（默认今日）、用量卡片接入 Token 单位（R2/R3）|
| WP7 | 监控端点路由展示 | [07-monitor-route.md](./07-monitor-route.md) | WP5（5.x 前端类型基线）| request_logs 增真实入站/出站路径（v5 迁移）、采集点透传、前端入站/出站改路径列（R1）|

构建顺序（PRD 建议）：R4 工具先行 → R6 监控修复 → R2 用量异步 → R3 时间筛选 → R1 路由路径 → R5 仪表盘单位 → R7 弹窗放大。即 WP5 先行（5.1/5.2 优先）→ WP6 / WP7 可并行。

## 任务清单

### WP5 前端工具与监控修复（前端）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 5.1 | Token 单位工具函数 `formatTokenCompact`（≈+两位小数+万/亿，就近取档）+ 单测 | 前端 | — | 13,14,15,16 |
| 5.2 | 稳定时间段计算纯函数（按天对齐、可记忆化）+ 单测 | 前端 | — | 19,20 |
| 5.3 | 修复 ranged 监控 queryKey 漂移导致的"一直加载"（接入 5.2，去除 `Date.now()` 进 key）| 前端 | 5.2 | 19,20 |
| 5.4 | 仪表盘"Token（今日）"接入 5.1 辅助单位（`StatCard` hint）| 前端 | 5.1 | 17 |
| 5.5 | 历史记录弹窗放大（宽度 + 表格滚动区）| 前端 | — | 21 |

### WP6 用量统计增强（后端 + 前端）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 6.1 | `sync_session_usage` 异步化：`async` 命令 + `spawn_blocking`，重 IO 不阻塞主线程 | 后端 | — | 6,7,8 |
| 6.2 | 用量统计时间筛选器（默认今日，今日/7天/30天/全部）驱动 start/end 查询 + 用量卡片接入 5.1 单位 | 前端 | 5.1,5.2 | 9,10,11,12,18 |

### WP7 监控端点路由展示（后端 + 前端）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 7.1 | v5 迁移：`request_logs` 增 `inbound_path`/`upstream_path`；`RequestLog` 模型 + 仓储插入/查询同步 | 后端 | — | 1,2,3,5 |
| 7.2 | 采集点透传真实入站/出站路径（`RequestMeta`/`RequestRecord`/聚合器明细 + `request-logged` 事件）| 后端 | 7.1 | 1,2,3 |
| 7.3 | 前端类型 + 监控表"入站/出站"改路径列（协议/上游主机降级为辅助；旧行按协议兜底）| 前端 | 7.1,7.2 | 1,2,4,5 |

## 提交策略（第二轮）

按工作包/模块/目录分批提交，使用 git-commit-bot：
- WP5：`5.1 工具函数 + 单测` 一组、`5.2/5.3 监控修复 + 单测` 一组、`5.4/5.5 展示细节` 一组。
- WP6：`6.1 后端异步化` 一组、`6.2 前端时间筛选 + 单位` 一组。
- WP7：`7.1 迁移+模型+仓储` 一组、`7.2 采集点+事件` 一组、`7.3 前端展示` 一组。

---

# TASKS（第三轮）— 端点熔断 + 智能切换（请求驱动）

> 关联 PRD：[PRD-3.md](./PRD-3.md) · 子文档：[08-endpoint-resilience.md](./08-endpoint-resilience.md)
> 原始需求：[需求3.txt](./需求3.txt) · 参考：ccNexus（旧版 Go）/ cc-switch（Rust 主蓝本）
> 背景：在现有「请求内重试 + round-robin 轮换」之上叠加每端点熔断器（请求驱动、惰性时间戳恢复、**无后台定时轮询**）。

## 工作包总览

| 阶段 | 工作包 | 子文档 | 前置 | 说明 |
|---|---|---|---|---|
| WP9 | 端点熔断 + 智能切换 | [08-endpoint-resilience.md](./08-endpoint-resilience.md) | 无 | 三态熔断器(请求驱动/惰性恢复/半开单许可/双触发)、错误分类、handle_proxy 选路集成、命令+事件、前端实时展示 |

## 任务清单（WP9）

| 任务编号 | 标题 | 所属层 | 前置 | PRD Story |
|---|---|---|---|---|
| 9.1 | `circuit_breaker.rs`：三态机 + 许可协议 + 双触发 + `BreakerRegistry`/`select_candidates` + 单测 | 后端 | — | 2,3,4,5,7,8,14 |
| 9.2 | `rotation.rs` 增 `categorize` 错误分类（Retryable/NonRetryable/ClientAbort）+ 单测 | 后端 | — | 6,15 |
| 9.3 | `ProxyState` 增 breakers；`handle_proxy` 集成（is_available 跳过/allow_request 许可/record 三路回传/全Open兜底/显式绕过/转换发事件）| 后端 | 9.1,9.2 | 2,3,5,6,7,8,9,12 |
| 9.4 | 命令 `get_endpoint_health` + 事件 `endpoint-health-changed` + 注册/前端封装 | 后端+前端 | 9.3 | 10,11,12 |
| 9.5 | 前端实时展示：仪表盘端点列表健康灯 + 端点页熔断徽标/连续失败/最近错误 + 单测 | 前端 | 9.4 | 10,11,12 |

构建顺序：9.1/9.2（纯逻辑+分类，可并行）→ 9.3（选路集成）→ 9.4（命令+事件）→ 9.5（前端展示）。

> 设计变更说明：早前「定时轮询 + 主动探测器复用」草案已废弃，改为 cc-switch 式**请求驱动 + 惰性恢复**（无后台任务、无自动探测；`test_endpoint` 手动探测保留不变）。

## 提交策略（第三轮）

- `9.1/9.2 熔断器+错误分类（纯逻辑+单测）` 一组；`9.3 handle_proxy 集成` 一组；`9.4 命令+事件` 一组；`9.5 前端展示` 一组。
