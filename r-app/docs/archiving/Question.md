问题汇总：

1：获取模型端点的路由，/v1/models ，依据当前启用的 所有端点 返回模型列表。

2：各个端点，应该显示各个端点应该有的模型，列表布局显示出来，卡片grid布局悬停在可用性上使用浮层卡片显示模型列表

3：添加端点，应该有自定义模型和刷新获取模型填入的功能 ，选中的模型才展示在模型列表中

![image-20260606175643575](Question.assets\image-20260606175643575.png)



4:研究E:\myCode\cc-switch ，ccswitch 中对 UA 的传递，当前软件是否控制ua传递

5：添加端点的中有一个开关，是否启用代理 ，设置中添加一个代理地址

![image-20260606180635654](Question.assets\image-20260606180635654.png)

6：当前如果添加的是gpt端点，然后gpt端点 /v1/chat/completions 入站的时候，应该正确发送到gpt端点，当前没有这个端点路由，需要添加。





方案：

问题1 → 定了:补 /v1/models,复用已有的 fetch_models 逻辑,不重写。

问题6：Claude 入站为主(Claude Code 发 /v1/messages,保持现状),但 OpenAI 客户端发 /v1/chat/completions
  时要能正确走到 OpenAI 端点;/v1/models 照补

最小版:OpenAI 入站只透传到 OpenAI 端点(请求/响应都不转换,直接转发)



决定问题 2、3

方案甲:models[] 是"这个端点对外暴露/已选的模型清单",只喂给 /v1/models 和 UI 展示;真正转发时透传客户端
  model。这样问题2(分组展示)、问题3(多选)、问题1(/v1/models 聚合已选)三者自然统一,且不破坏现有轮换。

代理粒度(问题5)：端点级开关后置。除非你有"部分端点直连、部分走代理"的明确刚需



  #  │                                                  最终方案                                                  │
  ├─────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 1   │ /v1/models HTTP 路由复用 fetch_models,聚合各端点的已选/锁定模型对外公布                                    │
  ├─────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 2   │ 端点 list/grid 双视图;grid 卡片 hover 可用性 → 浮层(限高可滚动)显示该端点模型                              │
  ├─────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 3   │ 端点加 models[](聚合清单);model 重定义为可选锁定模型(填=锁死覆盖,空=透传);表单加 自定义输入 + 刷新拉取候选 │
  │     │  + 多选标签云                                                                                              │
  ├─────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 4   │ 高级设置两个 UA(Codex / claude-cli),按上游端点格式选用,填=覆盖 空=透传                                     │
  ├─────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 5   │ AppConfig.proxy_url(全局地址)+ endpoint.use_proxy(端点开关);转发层维护 直连/代理 双 client                 │
  ├─────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 6   │ 按入站 path 识别格式:/v1/messages=Claude 入站(现状),/v1/chat/completions=OpenAI 入站(最小版,仅透传到       │
  │     │ OpenAI 端点);轮换候选按入站格式过滤                                                                        │
  └─────┴─────────────────────────────────────────────────────────────────────────────────────────
